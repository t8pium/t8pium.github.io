# Academics section notes

The site now injects an Academics section after the About section through `script.js`.

Current displayed values:

- IELTS: 8.0
- SAT: 1390
- APs: 5 APs

Update these values inside `ensureAcademicsSection()` in `script.js` when scores or AP details need to change.
